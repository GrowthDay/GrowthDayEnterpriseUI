import { FC } from 'react'
import BlockContainer from '../components/BlockContainer'
import { ReportComponentProps } from '../types'

const Line: FC<ReportComponentProps> = (props) => {
  return (
    <BlockContainer>
      <></>
    </BlockContainer>
  )
}

export default Line
